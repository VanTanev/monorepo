{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["MY_APP_ID"],
      "passThroughEnv": ["PORT"],
      "outputs": ["dist"]
    }
  }
}
